# Samples

Keep only redistributable, authorized fixtures here. Raw firmware extensions are
ignored by Git by default. Document provenance, license, processor, memory map,
compiler, and expected hashes for every committed fixture.

