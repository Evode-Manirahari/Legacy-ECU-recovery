{
  "schema_version": 1,
  "sample_id": "temperature_controller_v1",
  "title": "Temperature threshold controller",
  "source": "source/temperature_controller.c",
  "probe_parameters": ["temperature", "threshold", "unused"],
  "expected_functions": ["main", "sample_probe", "temperature_fan_on"],
  "expected_function_roles": {
    "main": "Runs embedded behavior self-tests and returns zero only when all pass.",
    "sample_probe": "Adapts the three-argument probe ABI to the controller.",
    "temperature_fan_on": "Returns one only when temperature is strictly above threshold."
  },
  "classification_functions": ["temperature_fan_on"],
  "expected_call_edges": [
    {"caller": "main", "callee": "sample_probe"},
    {"caller": "sample_probe", "callee": "temperature_fan_on"}
  ],
  "expected_constants": [0, 1],
  "expected_behavior": [
    {"inputs": {"temperature": 91, "threshold": 90, "unused": 0}, "output": 1},
    {"inputs": {"temperature": 90, "threshold": 90, "unused": 0}, "output": 0},
    {"inputs": {"temperature": -20, "threshold": 10, "unused": 0}, "output": 0}
  ]
}
